import React from "react";

function ExportToExcel({myExportData}) {
    const handleExport=async(myExportData)=>{
        await ex
    }
  return <div onClick={()=>handleExport(myExportData)}>Xuất thành file Excel</div>;
}

export default ExportToExcel;
